# Benchmark with respect to other frameworks

This folder contains setup and codes to run the benchmark of fortjs with respect to other framework.

# Installation

`npm install`

# Run benchmark

```
npm run benchmark
```
